(DONE) Write a README file.

(DONE) Remove compareCNVs.py

(DONE) Replace the CNV_raw_table.txt in the Supplementary Files with the version in the input directory.

(DONE) Replace the children_table.txt in the Supplementary Files with the version in the input directory.

(DONE) Replace Supplementary Figure 1 with the one generated here.

(DONE) Rename the environment.

(DONE) Add the Matplotlib line to all graph generating scripts to remove the dependence on DISPLAY.

(DONE) Add the options to the Makefile that delete target files on error.

(DONE) Updated the parameters in the figISBSignalPower.py to match the latest samples.
